<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="VitaHusApp.Pages.VideoMenu"
             Title="VideoMenu">
    <StackLayout>
        <toolkit:MediaElement 
            x:Name="mediaElement"
            Source="{Binding VideoUrl}"
            WidthRequest="300"
            HeightRequest="200" />

        <!-- Brugerkontroller til afspilning, pause og stop -->
       <!-- <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
            <Button Text="Play" Clicked="OnPlayClicked"/>
            <Button Text="Pause" Clicked="OnPauseClicked"/>
            <Button Text="Stop" Clicked="OnStopClicked"/>
        </StackLayout> -->
    </StackLayout>
</ContentPage>
